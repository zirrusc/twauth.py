# coding: utf-8

import tweepy
import os
import sys
import twauth

tokenPath = os.getcwd() + '/sample-token'

def init():
	if os.file.exists(tokenPath):
		# Read access token file and authenticate this application.
		
def post(text):
	
